<header>
    <nav class="navbar navbar-expand-lg">
    <%= link_to "りもーとレッスン♪|管理画面", admin_index_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
        <li class="nav-item active">
            <%= link_to "ホーム", admin_index_path, class: "nav-link" %>
        </li>
        <% if admin_signed_in? %>
            <li class="nav-item active">
                <%= link_to 'アカウント編集', edit_admin_registration_path, class: 'nav-link' %>
            </li>
            <li class="nav-item active">
                <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete, class: 'nav-link' %>
            </li>
        <% else %>
            <li class="nav-item active">
                <%= link_to 'ログイン', new_admin_session_path, class: 'nav-link' %>
            </li>
        <% end %>
        </ul>
    </div>
    </nav>
</header>